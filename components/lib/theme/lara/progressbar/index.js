export default `
.p-progressbar {
    border: var(--p-progress-bar-border);
    height: var(--p-progress-bar-height);
    background: var(--p-progress-bar-bg);
    border-radius: var(--p-border-radius);
}
.p-progressbar-value {
    border: 0 none;
    margin: 0;
    background: var(--p-progress-bar-value-bg);
}
.p-progressbar-label {
    color: var(--p-progress-bar-value-text-color);
    line-height: var(--p-progress-bar-height);
}
`;
